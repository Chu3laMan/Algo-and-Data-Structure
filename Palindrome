import java.util.*;

class Program {
  public static boolean isPalindrome(String str) {
		String r = "";
        for(int j = str.length()-1; j >= 0; j--) {
            char ch = str.charAt(j);
            r += ch;
        }
        
        return str.equals(r);
  }
}
